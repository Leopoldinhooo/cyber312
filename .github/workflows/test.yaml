name: Test runner

on:
  push:
    branches:
     - main

jobs:
  test-runner:
    runs-on:
     - self-hosted
     - linux
     - arm64
    steps:
     -name: Verification
      run: echo "Test de fonctionnement du runner !"

     -name: tests de commandes
      run:|
       echo "Runner OK"
       hostname
       whoami
       uname -a
